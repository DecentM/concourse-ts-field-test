image_resource:
  type: registry-image
  source:
    repository: concourse/oci-build-task
platform: linux
run:
  path: build
inputs:
  - name: concourse-ts-field-test
    path: .
outputs:
  - name: image
